/* SPDX-License-Identifier: GPL-2.0 WITH Linux-syscall-note */
/*
 * UAPI-compatible <linux/compiler_types.h> stub.
 *
 * FreeLinX/drivers vendored copy.  Linux' own <linux/compiler_types.h> is a
 * kernel-internal file that is not part of the UAPI; distro kernel-header
 * packages ship an empty stub in its place so UAPI headers that include it
 * (e.g. <linux/stddef.h>) compile for userspace.  The attribute macros those
 * headers actually need come from our vendored <linux/compiler.h>.
 */
#ifndef _LINUX_COMPILER_TYPES_H
#define _LINUX_COMPILER_TYPES_H

#endif /* _LINUX_COMPILER_TYPES_H */